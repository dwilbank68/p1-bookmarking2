<p id="notice"><%= notice %></p>

<%= link_to @topic, class:'list-group-item' do %>
    <%= @topic.name %>
    <span class="badge"><%= @topic.bookmarks.count %></span>
<% end %>

<%= link_to 'Back', topics_path %>
<ul class="list-inline">
  <%= render @topic.bookmarks %>
</ul>